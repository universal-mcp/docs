---
title: "Store"
---

Stores handle credential and sensitive data management. Supported store types include:

MemoryStore (in-memory, for testing)

EnvironmentStore (uses environment variables, ideal for CI/CD)

KeyringStore (uses system keyring for secure storage)

Stores provide a consistent interface for setting, retrieving, and deleting credentials.